// Vercel Serverless Function entry point
// Vercel's @vercel/node runtime will invoke this handler
// instead of calling app.listen() directly.
import app from "../src/app.js";

export default app;

